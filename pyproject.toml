[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "ichimoku-framework"
version = "0.1.0"
description = "Ichimoku research, backtesting, and execution framework"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]

